version: "3.9"

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: findone
      POSTGRES_PASSWORD: findone  # dev-only — use .env for any real environment
      POSTGRES_DB: findone
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U findone"]
      interval: 5s
      timeout: 5s
      retries: 5

  vault:
    image: hashicorp/vault:1.16
    environment:
      VAULT_DEV_ROOT_TOKEN_ID: dev-root-token  # dev mode only — never use in production
      VAULT_DEV_LISTEN_ADDRESS: "0.0.0.0:8200"
    ports:
      - "8200:8200"
    cap_add:
      - IPC_LOCK  # prevents Vault secrets from being swapped to disk

  backend:
    build: ./backend
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://findone:findone@db:5432/findone
      VAULT_ADDR: http://vault:8200
    depends_on:
      db:
        condition: service_healthy
      vault:
        condition: service_started
    volumes:
      - ./backend:/app

volumes:
  postgres_data:
